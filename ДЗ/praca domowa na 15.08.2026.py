# 1.
#
# Роутер проверяет обновления и получает список доступных версий:
# ["1.4.12", "1.10.2", "1.4.9", "1.2.30"]. Нужно найти самую свежую.
#
# Ловушка, ради которой существует задача: сравнивать версии как строки нельзя — "1.10.2" < "1.4.9"
# даёт True, ведь строки сравниваются посимвольно и «1» меньше «4».
#
# Правильный путь: разобрать каждую версию в кортеж чисел — и тогда поэлементное
# сравнение кортежей сработает именно так, как нумеруют версии.

# versions = ["1.4.12", "1.10.2", "1.4.9", "1.2.30"]
# tuple_versions = []
#
# for v in versions:
#     chisla = v.split('.')
#     nums = map(int, chisla)
#     tuple_versions.append(tuple(nums))
#
# print(max(tuple_versions))

# 2.
#
#  Робот-пылесос стартует в точке (0, 0) и смотрит на север.
#
# Прошивка исполняет команды-кортежи:
# 1. ("шаг", n) — проехать n клеток вперёд,
# 2. ("поворот", "направо") или ("поворот", "налево") — повернуться на месте.
#
# Определите, где робот закончит уборку, выполняя следующие команды:
#
# ```json
#
# [("шаг", 3), ("поворот", "направо"), ("шаг", 2), ("поворот", "направо"
# ), ("шаг", 1), ("поворот", "налево"), ("шаг", 4)]
# ```
#
# Ключевая идея: направление тоже храним кортежом-вектором — север это (0, 1), восток (1, 0), — тогда
# шаг превращается в прибавление вектора, а поворот направо — в перестановку (dy, -dx).
#

# x, y = 0, 0
# dx, dy = 0, 1
#
# commands = [("шаг", 3), ("поворот", "направо"), ("шаг", 2), ("поворот", "направо" ), ("шаг", 1), ("поворот", "налево"), ("шаг", 4)]




# 3.
#
# На складе интернет-магазина каждая запись учёта — кортеж (ряд, полка, товар).
# Роботу-кладовщику нужно уметь три вещи: найти все ячейки с нужным товаром,
# проверить, свободна ли конкретная
# ячейка под новую поставку, и распечатать опись ряда по порядку полок.
#
# На складе лежат следующие товары:
#
#     (1, 1, "клавиатуры"),
#     (1, 2, "мыши"),
#     (2, 1, "мониторы"),
#     (2, 3, "веб-камеры"),
#     (3, 2, "мыши"),
#
#
# Робот должен определить:
# 1. Где лежат мыши
# 2. Свободна ли ячейка (2, 2)
# 3. Какие предметы лежат на первом ряду
#
#
# items=[(1, 1, "клавиатуры"), (1, 2, "мыши"), (2, 1, "мониторы"), (2, 3, "веб-камеры"),(3, 2, "мыши")]
# product=input('искомый товар ->')
# tovar=[]
# for item in items:
#     if item[2]==product:
#         tovar.append((item[0], item[1]))
# print(product,'находится в:',tovar)
#
#
# ryad=int(input('проверяемый ряд ->'))
# polka=int(input('проверяемая полка ->'))
# cell = (ryad, polka)
# svobodna=True
# for item in items:
#     if item[0] == cell[0] and item[1] == cell[1]:
#         svobodna=False
# if svobodna:
#     print("Ячейка свободна")
# else:
#     print("Ячейка занята")
#
# row=int(input('товары в ряде ->'))
# tovary=[]
# for item in items:
#     if item[0]==row:
#         tovary.append(item)
# print("В ряду", row, "лежат:", tovary)

# задачки могут быть трудные на подумать, порешайте за ранее, не тяните до последнего