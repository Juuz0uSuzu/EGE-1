# 2. Дан кортеж t длинной N. Найдите максимально длинную подпоследовательность подряд
# идущих одинаковых элементов (т.е. длину максимального блока одинаковых подряд элементов).
#
# max_len = 1
# cur_len = 1
# t=(1,10,10,10,2,2,3,4,20,1)
#
# for i in range(len(t)-1):
#     if t[i] == t[i+1]:
#         cur_len += 1
#     else:
#         max_len = max(max_len, cur_len)
#         cur_len = 1
# print(max_len)

# Дан список кортежей, представляющих книги в библиотеке в
# формате (название, год_издания):
# books = [("Война и мир", 1865), ("1984", 1949), ("Гарри Поттер", 1997),
# ("Война и мир", 1865)]
# Распакуйте кортежи и создайте список названий книг, изданных после 1900 года.
# Создайте список годов издания, уменьшенных на 100 лет.
# Упакуйте названия книг и их годы издания в новый список кортежей, где
# к названию каждой книги добавляется префикс "Классика: "
# для книг до 1950 года.

# books = [("Война и мир", 1865), ("1984", 1949), ("Гарри Поттер", 1997), ("Война и мир", 1865)]
#
# new_books=[]
# for book, year in books:
#     if year > 1900 :
#         new_books.append(book)
# print(new_books)
#
# ages=[]
# for raspakovka in books:
#     book, year = raspakovka
#     ages.append((year-100))
# print(ages)
#
# before1950 = []
# after1950 = []
# for book, year in books:
#     if year < 1951 :
#         before1950.append(('Классика:'+book, year))
#     else:
#         after1950.append((book, year))
# print(before1950, after1950)

# Пиксель хранится кортежем color = (120, 200, 50).
# Фильтр «негатив» заменяет каждую компоненту на 255 − значение. Проверьте,
# что все компоненты корректны (0–255),
# и получите пиксель-негатив. Помните: кортеж изменить нельзя — только собрать новый.

# color = (120, 200, 50)
# c1,c2,c3 = color
# c1=255-c1
# c2=255-c2
# c3=255-c3
# f=(c1,c2,c3)
# for i in f:
#     if 0<i<255:
#         print(f)
#         break



# На двух упаковках даты в формате (день, месяц, год): d1 = (21, 3, 2026) и d2 = (5, 11, 2025).
# Определите, какой продукт съесть первым.
# Python сравнивает кортежи поэлементно слева направо,
# поэтому достаточно перепаковать дату в порядок (год, месяц, день) —
# и обычный знак < сравнит даты правильно.

# d1,d2 = (21, 3, 2026),(5, 11, 2025)
# d1=d1[::-1]
# d2=d2[::-1]
# if d1<d2:
#     print(d1)
# else:
#     print(d2)


# Касса получила покупки кортежами (название, цена, количество). Напечатайте чек построчно и итог.
# items = [("Хлеб", 45, 2), ("Молоко", 89, 1), ("Сыр", 560, 3)]
# total=0
# items = [("Хлеб", 45, 2), ("Молоко", 89, 1), ("Сыр", 560, 3)]
# for item in items:
#     product, price, quantity = item
#     total += price * quantity
#     print(f"{product} - {price} - {quantity}")
# print(total)


# Секундомер отдал результаты лучшего круга кортежами (фамилия, секунды).
# Определите победителя одним проходом, без сортировки.
# results = [("Леклер", 1.20), ("Хемильтон", 1.28), ("Ферстаппен", 1.22)]
# t=[]
# for raspakovka in results:
#     name, time = raspakovka
#     t.append((time, name))
# print(min(t))


# Прогноз температуры на неделю:
# week = (19, 21, 20, 22, 24, 28, 27).
# Распакуйте кортеж со звёздочкой так, чтобы
# пять будней попали в один список, а суббота и
# воскресенье — в отдельные переменные,
# и выясните, правда ли на выходных теплее в среднем.


# week = (19, 21, 20, 22, 24, 28, 27)
# *s, s1, s2 = week
# s3=s1,s2
# print(s, s3)
# flag=False
# temp1=(sum(s)/len(s))
# temp2=(sum(s3)/len(s3))
# if temp2>temp1:
#     print('На выходных действительно теплее')

# Операции по карте за неделю — кортежи (знак, сумма).
# Посчитайте итоговое изменение баланса и найдите самую крупную трату.
# ops = [("+", 50000), ("-", 3200), ("-", 15000), ("+", 12000), ("-", 8990)]

# bal=0
# top=0
# ops = [("+", 50000), ("-", 3200), ("-", 15000), ("+", 12000), ("-", 8990)]
# for raspakovka in ops:
#     operation, amount = raspakovka
#     if operation == "+":
#         bal += amount
#     elif operation == "-":
#         bal -= amount
#         if amount > top:
#             top = amount
# print(bal, top)

# 22. Метеозонд передал замеры кортежами (час, температура):
# `readings = [(0, 15), (1, 14), (2, 11), (3, 12), (4, 8)]`.
# Синоптикам важен самый резкий перепад между соседними замерами
# — найдите его величину и между какими часами он случился.

# readings = [(0, 15), (1, 14), (2, 11), (3, 12), (4, 8)]
# best_diff=0
# best_h1=0
# best_h2=0
# for i in range(len(readings)-1):
#     h1, t1 = readings[i]
#     h2, t2= readings[i+1]
#     diff = abs(t1-t2)
#     if diff > best_diff:
#         best_diff = diff
#         best_h1 = h1
#         best_h2 = h2
# print(best_h1, best_h2, best_diff)


# Профиль печати хранится кортежем config = ("PLA", 210, 60, 0.2)
# — пластик, температура сопла, температура стола, высота слоя.
# Кортеж не изменить, но новый пластик требует сопло 215.
# Соберите новый профиль из кусков старого срезами.

# config = ("PLA", 210, 60, 0.2)
# new_config= config[:1]+(215,)+config[2:]
# print(new_config)
